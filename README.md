# Client Starter Kit

Простой starter kit для создания клиентских приложений, использующих данные из админ-панели Axon Dashboard.

## 🚀 Особенности

- **Next.js 15** с App Router
- **Tailwind CSS 4** для стилизации
- **TypeScript** для типобезопасности
- **Авторизация** через `@igorchugurov/auth-sdk` (без ролей, только зарегистрированный пользователь)
- **Работа с сущностями** через `@igorchugurov/public-api-sdk`
- **Темная/светлая тема** с переключением
- **Автоматическая публикация** в Vercel

## 📋 Требования

- Node.js 18+
- pnpm (рекомендуется) или npm
- Supabase проект с настроенной админ-панелью
- Project ID из админ-панели

## 🛠️ Установка

1. Клонируйте или скопируйте этот проект
2. Установите зависимости:

```bash
pnpm install
```

3. Создайте файл `.env.local` на основе `.env.example`:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
NEXT_PUBLIC_PROJECT_ID=your_project_id
```

4. Запустите dev сервер:

```bash
pnpm dev
```

## 📁 Структура проекта

```
client-starter-kit/
├── app/
│   ├── layout.tsx              # Root layout с загрузкой entityDefinitions
│   ├── page.tsx                # Главная страница с блоками entityDefinitions
│   ├── login/                  # Страница логина
│   ├── profile/                # Страница профиля пользователя
│   ├── entities/
│   │   └── [slug]/             # Список сущностей
│   │       └── [instanceSlug]/ # Детали сущности
│   └── auth/                   # OAuth и сброс пароля
├── components/
│   ├── providers/              # React провайдеры (Auth, Query, Theme, EntityDefinitions)
│   ├── ui/                     # UI компоненты (shadcn/ui)
│   ├── Navbar.tsx              # Навигация с темой и профилем
│   ├── Sidebar.tsx             # Боковая панель с entityDefinitions
│   ├── EntityListClient.tsx    # Компонент списка сущностей
│   └── EntityDetailClient.tsx  # Компонент деталей сущности
├── lib/
│   ├── utils/                  # Утилиты (slug, entity-mapping)
│   ├── supabase/               # Supabase клиенты
│   └── auth/                   # Утилиты авторизации
└── middleware.ts               # Next.js middleware для авторизации
```

## 🔐 Авторизация

Приложение использует `@igorchugurov/auth-sdk` для авторизации:

- **Логин** через email/password
- **OAuth** (Google, GitHub)
- **Сброс пароля**
- **Профиль пользователя** (без ролей)

## 📊 Работа с сущностями

Все `entityDefinitions` загружаются в `layout.tsx` через `getAllEntityDefinitions()` одним запросом и кешируются:

- На **сервере**: кеш SDK (TTL 5 минут)
- На **клиенте**: React Query кеш

### Роутинг

- `/` - Главная страница с блоками entityDefinitions
- `/entities/[slug]` - Список экземпляров сущности
- `/entities/[slug]/[instanceSlug]` - Детали экземпляра

Где:
- `slug` - URL-friendly идентификатор entityDefinition (генерируется из `name`)
- `instanceSlug` - URL-friendly идентификатор instance (генерируется из поля `name`)

## 🎨 Темы

Приложение поддерживает переключение между светлой и темной темой через навбар.

## 🖼️ Изображения

Настроена поддержка изображений из Supabase Storage:

- Оптимизация через Next.js Image
- Поддержка форматов AVIF и WebP
- Адаптивные размеры

## 🚀 CI/CD и Деплой на Vercel

Проект настроен для автоматической публикации в Vercel с CI/CD через GitHub Actions.

### Настройка GitHub Actions

1. **Создайте Secrets в GitHub** (Settings → Secrets and variables → Actions):
   - `NEXT_PUBLIC_SUPABASE_URL` - URL вашего Supabase проекта
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Anon ключ Supabase
   - `NEXT_PUBLIC_PROJECT_ID` - ID проекта из админ-панели

2. **CI/CD автоматически запускается** при:
   - Push в `main` или `master` ветку
   - Создании Pull Request

3. **CI проверяет**:
   - Линтинг кода (`pnpm lint`)
   - Проверку типов (`pnpm type-check`)
   - Сборку проекта (`pnpm build`)

### Настройка Vercel

1. **Подключите репозиторий к Vercel**:
   - Зайдите на [vercel.com](https://vercel.com)
   - Нажмите "Add New Project"
   - Импортируйте ваш GitHub репозиторий

2. **Настройте переменные окружения** в Vercel:
   - `NEXT_PUBLIC_SUPABASE_URL`
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
   - `NEXT_PUBLIC_PROJECT_ID`

3. **Деплой произойдет автоматически** при:
   - Push в `main` ветку
   - После успешного прохождения CI проверок

4. **Или используйте Vercel CLI**:
   ```bash
   npm i -g vercel
   vercel
   ```

### Конфигурация Vercel

Проект использует `vercel.json` для настройки:
- Framework: Next.js
- Build Command: `pnpm build`
- Install Command: `pnpm install`
- Regions: `iad1` (можно изменить в `vercel.json`)

## 📝 Настройка

### Переменные окружения

Создайте `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_PROJECT_ID=your-project-id
```

### Кастомизация

1. **Стили**: Измените `app/globals.css` для кастомизации темы
2. **Компоненты**: Модифицируйте компоненты в `components/` под ваши нужды
3. **Роутинг**: Добавьте новые страницы в `app/`

## 🔧 Разработка

```bash
# Запуск dev сервера
pnpm dev

# Сборка для production
pnpm build

# Запуск production сервера
pnpm start

# Линтинг
pnpm lint
```

## 📚 Документация

- [Next.js Documentation](https://nextjs.org/docs)
- [Tailwind CSS Documentation](https://tailwindcss.com/docs)
- [Auth SDK Documentation](../../auth-sdk/README.md)
- [Public API SDK Documentation](../../public-api-sdk/README.md)

## 🤝 Поддержка

Для вопросов и проблем создайте issue в репозитории проекта.

## 📄 Лицензия

MIT

